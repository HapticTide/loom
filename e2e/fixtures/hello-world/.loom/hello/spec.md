# Hello World CLI

## 概述
创建一个简单的 Node.js CLI 工具，运行后输出问候语。

## 功能需求

### 1. 默认问候
运行 `node index.js` 输出 `Hello, World!`

### 2. 自定义名字
运行 `node index.js --name Alice` 输出 `Hello, Alice!`

## 技术约束
- 纯 Node.js，不使用任何外部依赖
- 包含 package.json
- 单个 sprint 完成
